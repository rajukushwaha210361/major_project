
<%layout("/layouts/boilerplate")%>
    <h1>All listing</h1>
    <div class="row cardclass row-cols-1 row-cols-md-2 row-cols-lg-3">
        <%for(let lists of allListing){%>
        <a href="/listing/<%=lists._id%>" class="listing-link">
        <div class="card col m-3" style="width: 22rem;">
            <img src="<%=lists.image%>" class="card-img-top" alt="listing-image" height="200px">
            <div class="card-img-overlay">HII</div>
            <div class="card-body ">
              <p class="card-text">
                <%=lists.title%> <br>
                ₹<%= (lists.price ? lists.price.toLocaleString("en-IN") : "0") %>
            </p>
            </div>
          </div>
        </a>
          <%}%>
        </div>
        
        
    <!-- <%for(let lists of allListing){%>
        <ul>
            <li><a href="/listing/<%=lists._id%>"><%=lists.title%></a></li>
        </ul>        
        <%}%> -->
    
